import {TSurveyModalButtonPosition} from "../../survey-modals.models";

export interface ISurveyModalButtonOptions {
  visible?: boolean;
  closeOnClick?: boolean;
  position?: TSurveyModalButtonPosition;
}
